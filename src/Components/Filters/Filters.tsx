import React, { useState } from 'react';import styles from './Filters.module.css';import { useAppDispatch } from '../../hooks/redux';import { sortButton } from '../../store/reducers/TicketSlice';const Filters = () => {  const [isBtnFirstActive, setIsBtnFirstActive] = useState(true);  const [isBtnSecondActive, setIsBtnSecondActive] = useState(false);  const [isBtnThirdActive, setIsBtnThirdActive] = useState(false);  const dispatch = useAppDispatch();  const activeHandler1 = (): void => {    setIsBtnFirstActive(true);    setIsBtnSecondActive(false);    setIsBtnThirdActive(false);  };  const activeHandler2 = (): void => {    setIsBtnFirstActive(false);    setIsBtnSecondActive(true);    setIsBtnThirdActive(false);  };  const activeHandler3 = (): void => {    setIsBtnFirstActive(false);    setIsBtnSecondActive(false);    setIsBtnThirdActive(true);  };  return (    <ul className={styles.filters}>      <li className={`${styles.filter} ${isBtnFirstActive ? styles.isActive : null}`}>        <label htmlFor={'checkBoxText'} className={styles.checkBoxLabel}>          <span className={styles.buttonText}>Самый дешевый</span>        </label>        <input          id={'checkBoxText'}          type="checkbox"          className={`${styles.inputCheckBox}`}          onClick={() => {            activeHandler1();            dispatch(sortButton('дешевый'));          }}        />      </li>      <li className={`${styles.filter} ${isBtnSecondActive ? styles.isActive : null}`}>        <label htmlFor={'checkBoxText'} className={styles.checkBoxLabel}>          <span className={styles.buttonText}>Самый быстрый</span>        </label>        <input          id={'checkBoxText'}          type="checkbox"          className={`${styles.inputCheckBox}`}          onClick={() => {            activeHandler2();            dispatch(sortButton('быстрый'));          }}        />      </li>      <li className={`${styles.filter} ${isBtnThirdActive ? styles.isActive : null}`}>        <label htmlFor={'checkBoxText'} className={styles.checkBoxLabel}>          <span className={styles.buttonText}>Оптимальный</span>        </label>        <input          id={'checkBoxText'}          type="checkbox"          className={`${styles.inputCheckBox}`}          onClick={() => {            activeHandler3();            dispatch(sortButton('оптимальный'));          }}        />      </li>    </ul>  );};export default Filters;