import { createAsyncThunk } from '@reduxjs/toolkit';import axios from 'axios';import { ISearchId } from '../../Components/models/SearchId';export const fetchSearchId = createAsyncThunk('ticket/FetchSearchId', async (_, thunkAPI) => {  try {    const { searchId } = await axios      .get<ISearchId>('https://aviasales-test-api.kata.academy/search')      .then((r) => r.data);    return searchId;  } catch (e) {    return thunkAPI.rejectWithValue('Ошибка загрузки');  }});export const testFetch = async () => {  const { searchId } = await axios.get<ISearchId>('https://aviasales-test-api.kata.academy/search').then((r) => r.data);  return searchId;};